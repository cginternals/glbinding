#pragma once

#include <%abinding/no%a.h>


namespace %a
{

enum class %Aextension : int // %Aextension is not a type introduced by any Khronos API so far
{
    UNKNOWN = -1,
    %s
};

} // namespace %a
